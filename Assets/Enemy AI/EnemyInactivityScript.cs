using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyInactivityScript : MonoBehaviour
{
    // This script is to set enemies that are not on screen to an inactive state, so they do not roam around too much off screen
    void Start()
    {
        
    }

    private void OnTriggerEnter(Collider other)
    {
        
    }
}
